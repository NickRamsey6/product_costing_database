<h1>Stores</h1>

<% if @stores.any? %>
<ul>
  <% @stores.each do |store| %>
  <li><%= link_to store.name, store_path(store) %></li>
  <% end %>
</ul>
<% else %>
<p>There are no stores yet.</p>
<% end %>

<p><%= link_to "Add a store", new_store_path %></p>
